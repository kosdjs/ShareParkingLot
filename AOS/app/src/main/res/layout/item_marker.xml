<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:id="@+id/cl"
    android:background="@android:color/transparent"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <ImageView
        android:id="@+id/marker_id"
        android:layout_width="130dp"
        android:layout_height="130dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>
    <TextView
        android:id="@+id/marker_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="1000"
        android:fontFamily="@font/kakao_otf_bold"
        android:textColor="@color/black"
        android:textSize="32sp"
        android:layout_marginBottom="26dp"
        app:layout_constraintTop_toTopOf="@id/marker_id"
        app:layout_constraintLeft_toLeftOf="@id/marker_id"
        app:layout_constraintRight_toRightOf="@id/marker_id"
        app:layout_constraintBottom_toBottomOf="@id/marker_id"/>

</androidx.constraintlayout.widget.ConstraintLayout>